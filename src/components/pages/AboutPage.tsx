import { Container, Typography } from '@mui/material';

function AboutPage() {
  return (
    <Container>
      <Typography variant="h2">About Us</Typography>
      <Typography variant="body1">Learn more about our mission and activities.</Typography>
    </Container>
  );
}

export default AboutPage;